{% macro is_desktop(bool) %}
{{ bool ? '-desktop' : '-mobile' }}
{% endmacro %}

{% macro is_active(segment) %}
{% if craft.app.request.segments | first == segment %}active{% endif %}
{% endmacro %}

{% macro meta(title, description) %}

{% endmacro %}

{% macro vectorBg(block) %}
{{ block['vector'] and block.vector.one() ? block.vector.one().getUrl() : '' }}
{% endmacro %}